# TriadFS Benchmarks

Full benchmark methodology is available at:

- `docs/BENCHMARKS.md`

Highlights:

- Captures speed, memory, CPU approximation, transferred bytes, compression ratio, dedup savings, and cost estimate
- Stores benchmark outputs in PostgreSQL for trend and ranking analysis
- Exposes comparison APIs consumed by the frontend dashboard